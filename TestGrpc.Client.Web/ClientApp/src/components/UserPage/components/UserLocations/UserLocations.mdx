---
menu: Form
name: User Locations
route: /form/userlocations
---

import { Form } from 'react-final-form';
import arrayMutators from 'final-form-arrays'
import { Playground, Props } from 'docz'
import { Direction } from 'external/user_pb';
import UserLocations from './UserLocations.tsx'

# User Location

<Playground>
    <Form
        onSubmit={() => {}}
        mutators={{
            ...arrayMutators
        }}
        initialValues={{ 
            user: { 
                locations: [
                    { id: 1, name: 'One', lat: 10, long: -4 },
                    { id: 1, name: 'Two', lat: -10.123, long: 1.12312 },
                ]
            }
        }}
        render={p => (<>
            <UserLocations />
            <pre style={{fontSize: "14px"}}>{JSON.stringify(p.values, null, 2)}</pre>
        </>)}
    />
</Playground>

# Api

<Props of={UserLocations}></Props>