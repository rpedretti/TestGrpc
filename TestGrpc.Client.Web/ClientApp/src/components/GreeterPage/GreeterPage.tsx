import { Box } from '@material-ui/core';
import * as React from 'react';


const GreeterPageComponent = () => {
  return (
      <Box>
          hello
      </Box>
  );
};

export default React.memo(GreeterPageComponent);
